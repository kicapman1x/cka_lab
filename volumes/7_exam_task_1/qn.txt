In cluster ek8s, configure a hostPath PV named vol02833 with a size of 100 MB of local storage from the worker node host node01. The directory on the host should be /mnt/data. Create a PVC named claim-02833 that will reserve 90 MB of storage from the volume vol02833. Mount it to a Pod within a Deployment named frontend0113; the mount path within the container should be /usr/share/nginx/html. The name of the YAML file should be deploy.yaml.